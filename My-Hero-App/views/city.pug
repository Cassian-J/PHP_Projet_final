extends layout

block content
  section(class="box")
    h1 Create City #{title}
    
    form(method="POST")
      input(type="text" id="cityName" required autofocus placeholder="enter name city" class="entry")
      button(id="create_city" type="button" class="btn") Create city
        
  script(type='module' src='/javascripts/city.js')